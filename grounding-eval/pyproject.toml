[project]
name = "grounding-eval"
version = "0.1.0"
description = "Source-grounded LLM output evaluation benchmark"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[project.scripts]
grounding-eval = "src.cli:main"
